'use client';

// Temporarily disabled custom cursor to fix click issues
const CustomCursor = () => {
  // Return null to disable custom cursor completely
  return null;
};

export default CustomCursor; 